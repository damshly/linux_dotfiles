# prefix
unbind C-b
set -g prefix C-a
bind C-a send-prefix

set -g mouse on

set -g status-bg colour237          # nord0
set -g status-fg colour195          # nord4

set -g status-left-length 40
set -g status-right-length 120



set-option -g set-clipboard on
bind -T copy-mode-vi y send -X copy-pipe "wl-copy"


set -sg escape-time 0
set -g history-limit 10000

bind | split-window -h
bind - split-window -v
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind z resize-pane -Z

bind c new-window
bind & kill-window
bind n next-window
bind p previous-window
# bind , rename-window
# bind $ rename-session


setw -g mode-keys vi

set -g status-interval 1
set -g status-justify centre
set -g status-left-length 40
set -g status-right-length 80

set -g status-left '#[fg=cyan]   #[fg=white]#S \
#(ping -c1 1.1.1.1 >/dev/null 2>&1 && echo "🌐" || echo "⛔")\
#{pane_current_path}'

set -g status-right '#[fg=colour110]#(git -C #{pane_current_path} rev-parse --abbrev-ref HEAD 2>/dev/null) \
#{?pane_in_mode,#[fg=colour167][COPY],} \
#[fg=colour245]#(whoami) \
#{?client_prefix,#[fg=colour208][CMD],}\
#[fg=colour110]%H:%M'

bind r source-file ~/.tmux.conf \; display-message "  Reloaded!"

